/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TaskManagerApp from './taskManagarApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TaskManagerApp);
